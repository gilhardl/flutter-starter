import { FlutterExecutorOptions } from '../../lib/models/flutter-executor-options.model';

/**
 * Flutter clean Nx executor options.
 *
 * These are specific options passed to the `flutter clean` command.
 */
export interface FlutterCleanExecutorOptions extends FlutterExecutorOptions {}
